name: engine_auth
description: Engine Auth
version: 0.0.2
author:
homepage:

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
#  firebase_auth: ^0.18.3+1
  firebase_auth: ^1.0.1
  google_sign_in: ^5.0.0

  hooks_riverpod:
#  freezed_annotation:

  engine_db_utils:
    git:
      ref: master
      url: git@bitbucket.org:firebuzz_engine/engine_db_utils.git
  engine_utils:
    git:
      ref: master
      url: git@bitbucket.org:firebuzz_engine/engine_utils.git



#  engine_db_utils:
#    path: ../engine_db_utils
#  engine_utils:
#    path: ../engine_utils


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
#  freezed:



flutter:

